import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class StatementTest {
    private Statement subject;

//    @BeforeEach
//    void setup() { subject = new Statement(); }

//    @Test
//    void canPrintAStatementWithoutTransactions() {
//        assertEquals("date || credit || debit || balance", subject.printStatement());
//    }

//    @Test
//    void canPrintAStatementWithTransactions() {
//        subject.
//    }
}
// test for return statement
//    assertEquals("date || credit || debit || balance\n" + dtf.format(localDate) + " || " + 500.00 + "|| - || " + 500.00, subject.getTransactions());
